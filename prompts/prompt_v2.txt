You will be given a task to complete, a screenshot of the current state, and HTML of the current state.

Use the run_pw tool to interact with the state. The run_pw tool runs Python Playwright scripts that you will write.

Ensure your script is wrapped inside 'async def main(page):'. Your tool call should only contain the script, nothing else.

Your script should contain zero imports, comments, logging (print), or fallbacks (try, except). Your should script must only contain Playwright actions.

Prefer using 'page.get_by_role' in your scripts. When getting by the ARIA role, it helps to use the HTML.

Make gradual steps toward the task. Do not attempt to one-shot the task.